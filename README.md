Play Interval and Timeout
============================
Testing on the multiple interval objects and timeout object that created on the fly. These interval ojects and timeout object are used to animate dynamic elements.